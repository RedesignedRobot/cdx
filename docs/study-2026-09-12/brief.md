Outcome: a measured study of the last 48 hours of cdx execution and a ranked improvement plan for cdx and for the head's process. Write two files in this directory: study.md (what worked, what failed, where time and tokens went, with numbers from the logs) and plan.md (ranked changes with mechanism, expected saving per release cycle, risk, and a Gemini lane split with exclusive files under /Users/mas/code/cdx and the gate `bun run check`, plus a short process checklist for the head). Consumer: the owner, who will approve the plan and have the head run the implementation lanes. Prefer deletion and simplicity; every proposal names the code path it changes.

Material (all in this directory, all read-only except digests/): lanes-48h.tsv is the index of the 147 lanes (124 Gemini, 23 Astra/GPT; 118 work, 29 review) with engine, model, effort, rounds, state, exit and tokens. ledger-48h.json holds the full ledger entry per lane (accounts, round timings, failure notes, report paths, gates). jobs-48h.json holds the 55 detached jobs (release trains, walls) with exit codes and durations. logs/<lane>-r<n>.jsonl are the round transcripts (one JSON event per line: init, tool calls, tool results, agent messages, token usage) and logs/job-<name>.log are job logs; 806 files, 112 MB, so read them through targeted greps and slices, never whole. reports/<lane>-r<n>.md (and .partial.md for failed rounds) are the lane reports; briefs/ the briefs each lane received; specs/ the round specs (flags, gate, cwd, runtime); questions/ and control/ the question and control files; feed.log the JSON event journal with timestamps; spawn-audit.log the spawn record; usage.json and usage-gemini.json the quota snapshots. The cdx source is /Users/mas/code/cdx (cdx.ts, SKILL.md, README.md, hooks/); read it to ground every proposal in the code, never modify it. Context: the lanes ran Hyperscale release cycles 32 to 35 (fix lanes on worktrees, Gemini reviews, one wall and one train per release, retest lanes recording attempts into a QA register with `bun qa.ts validate` as the gate, portal lanes driving a Mac mini over ssh); the heads were Claude Code sessions.

Questions the study must answer with numbers:
1. Wall-clock and tokens per lane kind and engine; median and worst lanes; how much of each lane was reading versus acting.
2. Every failed or partial round and its cause class: Gemini transport interruptions, 503s, quota refusals, gate failures, max-runtime hits, unanswered questions, cwd or bare-tool gate mistakes, work finished before the failure. How many minutes and tokens each class cost, and what cdx could have done (auto-finish from the partial, retry policy, gate normalisation).
3. Rounds and lanes spent redoing done work: resumes after transport deaths, respawns, second reviews, walls thrown away by a later commit, trains that failed (jobs-48h.json) and why.
4. Retest lane honesty: lanes that replayed old evidence, cited refusals under success obligations or blocked on closed findings, and what in the brief or the tool would have prevented it.
5. Briefs: length versus outcome; instructions lanes ignored; instructions that were missing and cost a round.
6. Reviews: which found real defects (P1 and P2) and which found nothing; cost per real finding; whether Gemini reviews of Gemini lanes are worth their tokens.
7. Supervisor and delegation: how Astra lanes used Gemini children, idle time waiting on children, and whether a plain Gemini lane would have done the same work.
8. Head-side waste visible in the journal: polling, waits, repeated status checks, duplicate spawns, lanes left open.
9. Known gaps to include in the plan: refuse any lane-spawned child whose model resolves to gpt-6-astra; let a consult lane spawn read-only Gemini helpers; `cdx usage` prints stale "quota exhausted" advice for codex-2 while its weekly window shows 0 percent used (see usage.json and the advice line in cdx.ts).

Delegation: you may spawn Gemini children through your own cdx to digest slices of the transcripts, for example one child per 25 lanes: `cdx spawn cdx-study-<slice> --cd /tmp/cdx-study --effort high --bg --max-runtime 30 "<brief>"`, asking each for a digest at digests/<slice>.md with the per-lane numbers and quotes you need (wall-clock from first to last event, tokens, tool call counts by kind, failure events with their exact text, evidence of redone work). Children read only and write only under digests/. Never spawn a gpt or Astra child. Join every child before you write the study.

Constraints: no git commands; nothing modified outside this directory; transcripts contain environment variable names and paths, never copy a value that looks like a credential; finish within 120 minutes. Unslop voice: plain words, no em dashes, numbers over adjectives. End your report with the top five findings and the top five changes, each in one sentence.
