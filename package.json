{
  "name": "cdx-lanes",
  "version": "6.5.0",
  "license": "MIT",
  "type": "module",
  "bin": {
    "cdx": "./cdx.ts"
  },
  "repository": "github:RedesignedRobot/cdx",
  "description": "OpenAI Codex and Google Antigravity execution lanes for Claude Code.",
  "scripts": {
    "check": "tsc --noEmit && bun build cdx.ts --target=bun --outfile=/tmp/cdx-check.js && bun test cdx.test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
